class BaseBar extends BaseCustomControl {
    constructor({ element, container, attrs, types }) {
        super({
            element: element,
            container: container,
            attrs: attrs,
            types: types
        })
    }
}