class BaseButton extends BaseCustomControl {
    constructor({ element, container, attrs, cap }) {
        super({
            element: element,
            container: container,
            attrs: attrs,
            cap: cap
        })
    }
}